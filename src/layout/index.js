// Layout components for route organization
// Each layout is completely independent and should not be mixed

export { default as MainLayout } from './MainLayout';      // For public + student routes
export { default as AdminLayout } from './AdminLayout';    // For admin routes only
